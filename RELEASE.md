# Release Process

1. Bump version in `package.json` / `go.mod` / `RELEASE.md`
2. Update CHANGELOG.md
3. Create PR to `main`
4. CI must pass
5. Create GitHub Release with changelog

Tagging convention: `vMAJOR.MINOR.PATCH` (SemVer)
